package com.malcolm.imagesampling;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeImageCheckResourceIT extends ImageThumbnailResourceTest {

    // Execute the same tests but in native mode.
}